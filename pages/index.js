import React from 'react'

import Template from '../components/template'

const headOptions = {
  title: 'Home',
};

const Home = () => (
  <Template headOptions={headOptions}>

  </Template>
);

export default Home
